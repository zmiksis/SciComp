#include "function_to_integrate.h"
#include <cmath>

// This file is not actually part of the solution,
// but you need it for the main program to run

using namespace std;

double f(double x){
	return x * exp(x);
}
